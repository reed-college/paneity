"""
This is the settings_prod.py template. If you are deploying you
should copy this file to paneity/settings_prod.py.
paneity/settings_prod.py gets imported by paneity/wsgi_prod.py and
thats how those settings go in to effect
"""
from paneity.settings import *

SECRET_KEY = 'Change this to something unique'

DEBUG = False

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'paneity'
    }
}
